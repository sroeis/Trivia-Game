#pragma once
#include <vector>
#include <string>
#include <string>
//#include "IDataBase.h"

using std::vector;
using std::string;
class LoginManager
{
private:
	//IDataBase* m_dataBase;
	//vector<LoggedUser> m_loggedUsers;

public:
	//SignUpStatus signup(string, string, string);
	//LoginStatus login(string username, string password);
	//void logout(string);
};
